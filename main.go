package main

import (
	"SF_5.8/math"
	"fmt"
)

func main() {
	// выводим сумму двух передаваемых чисел(int)
	fmt.Println(math.Add(3, 5))
	//выводим разность между предыдущей суммой двух чисел и переданным числом(uint)
	fmt.Println(math.Sub(2))
}
